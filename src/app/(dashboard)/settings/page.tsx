import { Card, CardContent } from "@/components/ui/card";

export default function SettingsPage() {
  return (
    <Card>
      <CardContent className="py-12 text-center text-sm text-muted-foreground">
        기능 on/off, 카드 순서, 계정 관리 화면은 다음 단계에서 구현됩니다.
      </CardContent>
    </Card>
  );
}
