import { Card, CardContent } from "@/components/ui/card";

export default function HistoryPage() {
  return (
    <Card>
      <CardContent className="py-12 text-center text-sm text-muted-foreground">
        전체 진행 이력 화면은 다음 단계에서 구현됩니다.
      </CardContent>
    </Card>
  );
}
